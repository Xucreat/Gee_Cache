syntax = "proto3";
/*将 HTTP 通信的中间载体替换成 protobuf*/
package geecachepb;

option go_package = "/";

message Request {
    string group = 1;
    string key =2;
}

message Response {
    bytes value = 1;
}

service GroupCache {
    rpc Get(Request) returns (Response);
}
